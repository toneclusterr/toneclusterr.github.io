# 実装計画 - releaseページ index表示の修正

Releaseページのindex（目次）の表示が変であるとのご指摘を受け、Worksページと同様の表示になるように修正します。

## ユーザーレビューが必要な事項

- **特になし**: Worksページのスタイルに合わせる修正ですので、大きな仕様変更はありません。

## 変更内容

### CSS

#### [MODIFY] [releases.css](file:///c:/Users/IBM/toneclusterr.github.io/releases.css)

- `works.css` との差異を確認し、以下のスタイルを適用・同期します。
    - `.release h2` に `font-weight: bold;` を追加（`works.css` にのみ存在したスタイル）
    - `.toc` 関連のスタイル定義を `works.css` から完全にコピーし、潜在的な不一致（不可視文字や微細な差異）を排除します。

## 検証計画

### 自動テスト
- 現在ブラウザテスト環境が利用できないため、ファイル内容の整合性チェックを行います。

### 手動検証
- 修正後、`releases.html` を開き、index（目次）の表示が `works.html` と同様（フォントサイズ、リストマーカーなし、＋/−の挙動など）になっているか確認してください。
- また、各リリースのタイトル（h2）が太字になっていることも併せて確認してください。
